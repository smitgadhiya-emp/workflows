Every release we ship a pile of merged PRs and nobody turns it into anything a human can read. Devs get raw PR titles, clients get nothing, and leadership gets whatever the PM happened to remember in a Teams message. I want that fixed for real this time. Read every merged PR since the last release tag in the GitHub repo `smitempiricinfotech-wq/Multi-Audience-Generator` (branch `main`), sort each change into a type, then write three separate versions of the release notes for three different audiences, save all three to Google Drive, post the right version to the right Teams channel, and update the changelog file in the repo. The point is that every release lands with the right people in the right language, not one dump that nobody reads. Use the connected plugins (GitHub, Google Sheets/Drive, Microsoft Teams) and the existing Chrome session if it helps, or open a fresh one. Every date I mention is Asia/Kolkata.

Treat this the way someone who has shipped a bad release note before would. A wrong or missing breaking change in the client update is how you break somebody's integration and spend your weekend on a call. So err on the careful side the whole way through. And I mean every PR in the range gets looked at. If you sample, skim, or stop early because "it's mostly internal stuff," that's a fail. Coverage of the range has to be total or the notes are worthless.

Here's what I actually want out of this:

A clean read of everything that landed since the last release. Each change typed correctly. Three honest versions of the notes, each pitched at its audience. All three filed where people can find them. The right one posted to the right channel. And the repo changelog updated so the source of truth doesn't drift. No invented changes, no guessed impact, no fluff.

Finding the release boundary : Start by figuring out what "since the last release" actually means, because this is where it goes wrong. Find the latest release tag on `main`: the highest semantic version tag (vX.Y.Z), and skip anything that's a pre-release (any tag with a suffix like -rc, -beta, -alpha, or -next) and anything that's a draft. Write down the exact tag you picked and the commit it points at, so this can be rerun against the same baseline. Then the PR set is every PR merged into `main` after that tag's commit, up to the latest commit on `main` as of the run. Rules for the set, so two people building it land on the same list: only PRs actually merged into `main` count, open and closed-unmerged do not. If a PR was merged and then reverted inside the same range, drop both the original and the revert from the notes, but list them in an internal "reverted, not shipped" note so it's clear you saw them. If a PR is already contained in the last tag, it's not in the range. If you cannot resolve the last tag, or the range comes back empty, stop and tell me plainly. Do not invent a release.

Typing each change : Sort every PR into exactly one of these: feature, bug fix, performance improvement, breaking change, or internal. Use this order of evidence so it's repeatable: first the PR's labels, then its conventional commit type in the title (feat, fix, perf, refactor, chore, build, ci, test, docs), then the title and body text. When more than one type could fit, the most serious one wins, in this priority: breaking change beats feature beats bug fix beats performance improvement beats internal. So a bug fix that also breaks an API is a breaking change, full stop. Breaking change gets a stricter test, apply it, don't eyeball it: it's breaking if the PR carries a breaking label, or a `!` after the type (feat!:), or a `BREAKING CHANGE:` footer, or it removes or renames a public endpoint, changes a response shape, makes a previously optional request field required, or needs a migration to run. Internal means it only touches tooling, tests, CI, build, or internal packages and has zero user-visible effect. If the labels say one thing and the content clearly says another, trust the content only for the breaking-change test (a mislabeled breaking change is the dangerous one), otherwise go with the label and log the conflict. If a PR genuinely can't be typed from the evidence, do not guess. Park it as "Unclassified, needs review," keep it out of the user-facing and executive versions, and list it for a human to settle. For each PR record the number, title, author, merge date, the type you gave it, breaking yes or no, user-visible yes or no, the evidence you used (label, prefix, or text), and a one-line reason. Put that table at the bottom of the technical version.

Deciding the version number : Work out the next version off the last tag, objectively. If the range has any breaking change, bump the major. Else if it has any feature, bump the minor. Else bump the patch. If everything in the range is internal only, still cut the notes but say up front this is an internal-only release with nothing user-facing. Use that computed version as the title of all three documents and the changelog entry, and show how you got it (last tag plus the bump reason).

Writing the three versions : Same set of changes, three different reads, and they have to stay consistent with each other. The technical changelog is for the dev team: group changes by type, reference each PR by number and title, call out breaking changes and any migration steps at the top, and include that per-PR classification table. The user-facing update is plain language for clients: no PR numbers, no internal jargon, only changes a user would actually notice, features and fixes and perf wins described in terms of what's better for them, and every breaking change spelled out with what they need to do about it. The executive summary is one paragraph for leadership: what shipped, the business-relevant highlights, and whether anything needs customer communication. Hard consistency rules so the three don't drift: nothing appears in the user-facing or executive version that isn't in the technical version, internal-only changes stay out of the user-facing and executive versions entirely, and every breaking change appears in all three. Keep what you verified from the PRs separate from anything you're inferring, and if you're unsure whether a change is user-visible, treat it as internal and flag it rather than announcing something that isn't real.

Saving to Drive : Save all three as separate Google Docs in the `Engineering / Release Notes` folder in Google Drive. Name them exactly `Release <version> - Technical Changelog`, `Release <version> - User Update`, and `Release <version> - Executive Summary`. Each doc needs the version, the release date (run date, Asia/Kolkata), the compared range (last tag to current commit), and the body for its audience. If a doc for this version already exists in the folder, update it in place, do not create a second copy.

Posting to Teams : Post each version to its audience in the `Workflow test` team. Technical changelog goes to the `Dev Releases` channel, user-facing update goes to the `Product Updates` channel, executive summary goes to the `Leadership Updates` channel. Each post leads with the version and a link to its Drive doc, then a short lead-in, not the whole document pasted in. If any of those channels doesn't exist, post that version to the `Workflow test` channel instead and say clearly in your summary that you used the fallback so nobody assumes it went to the right place. One thing that matters most: if this release has any breaking change, put a "Breaking Changes, Action Required" block at the very top of the Dev Releases and Product Updates posts, loud, so people catch it in one glance instead of scrolling. If there are no breaking changes, say that plainly too.

Updating the changelog in the repo : Update `CHANGELOG.md` at the root of `smitempiricinfotech-wq/Multi-Audience-Generator`. Prepend a new section for this version with the release date and the changes grouped by type, newest on top, and do not touch or rewrite the existing entries below it. Do not push to `main` directly. Commit to a new branch named `release-notes/<version>` and open a pull request against `main` so a human can merge it. The changelog entry has to match the technical version, same changes, same types, same breaking-change callouts.

Ground rules, in case any of the above gets ambiguous

Accuracy beats completeness, and completeness beats polish. Don't invent anything: not a change, not its impact, not whether it breaks something, not who it affects. If the evidence for a PR is thin, park it as needs review instead of forcing it into a bucket. Keep verified facts separate from anything inferred, and any time your confidence on a classification or a breaking-change call is low, say why. If a channel, folder, tag, or file isn't where I said, tell me what you found instead of quietly working around it.

What finished looks like:
The resolved last tag and PR range, every PR in the range typed with its reasoning, the computed next version with its bump reason, the three Google Docs saved in `Engineering / Release Notes`, the three Teams posts in the right channels with the breaking-change block if one applies, and the open changelog PR against `main`. The counts have to reconcile: the number of changes in the technical version, the changelog entry, and the per-PR table all agree, and nothing in the user or exec version is missing from the technical one.

It's not done until every merged PR in the range has been read and typed, the reverted and unclassified ones are called out instead of hidden, all three docs are filed, all three posts are up in the right places, and the changelog PR is actually open.


Metadata:

1. Occupation / career (dropdown choice):
-> Software Developer
2. Occupation + workplace (one line, this is the persona voice):
-> Software developer or release lead at a product software company, responsible for shipping releases and communicating changes to developers, clients, and leadership.
3. Time to complete this workflow WITHOUT a model (minutes):
-> 240 minutes  
4. Times PER MONTH I run this workflow (decimal ok, 0.5 = every 2 months):
-> 6
5. Workflow difficulty 1-7 (1 easy, 7 hard):
-> 6