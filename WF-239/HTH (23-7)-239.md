Model - A 

Session Id : 019f934d-82b0-7c62-b1f7-922839e7049f

1. Overall task success - 4/7

It ran the whole thing autonomously and landed every deliverable: the audit sheet upserted with human Owner/Status preserved on the orders and products rows, the legacy-webhook route marked Resolved rather than deleted, Linear handled idempotently (2 open issues updated, new ones created including for the closed products issue, the deleted payments issue restored not duplicated, no dupes), 11 routes at or above 45 all linked, one Teams post after the writes, and a self-audit shown. The rigor is real, it ran a live runtime test to verify header duplication instead of assuming. But the analytical core lands far from the setup: 13 of 15 routes came back Unresolved where only one was meant to, it found a single wildcard-on-authenticated where the stack had four, it downgraded the orders headline to a duplicate, and it flagged one of the clean controls. Its duplicate-header reading is defensible and evidence-backed, but the persona wanted the effective policy after all three layers fight it out, and what came back is mostly "front door unprovable, can't say." Complete and rigorous, but the headline the audit exists to surface got hedged away.

2. Task accuracy, ignoring speed - 4/7

The process mechanics are basically flawless: upsert keyed on path plus method, human columns untouched, resolved-route retention, threshold applied, idempotent Linear, self-audit reconciled. The duplicate-ACAO finding on the stacked routes is a genuine insight and probably correct, browsers do reject duplicate origin headers, and it verified it at runtime rather than guessing. Where it loses ground is the resolution stance and its downstream effects. Treating every route without its own Nginx location as Unresolved-on-the-front-door, rather than concluding Nginx does not touch it and the app layer is therefore the effective policy, left the sheet dominated by Unresolved rows and knocked the intended wildcard routes (orders, checkout, admin export) out of the wildcard classification and, for admin export, below the issue threshold the setup specifically built it to clear. It also flagged the newsletter control. Strong on mechanics and one real subtlety, weak on delivering resolved policies and the intended findings.

3. Efficiency - 5/7

End-to-end time (minutes): ~26.5

Wrong actions / recovery: Essentially none. No dead ends, no retries, no wrong-target work in the logs.

It moved in a straight line: preflight all four systems, pin the commit, pull all three layers, run the runtime header check, resolve per route, write Linear, upsert the sheet, post once. The runtime test added time but it is on the critical path here, it is what let it resolve the header-precedence ambiguity with evidence instead of a coin flip.

4. Writing quality - 3/7

The chat-side summary is organized, but the Teams post is a dense wall of text held together by bullet separators and em dashes, which is exactly what I asked not to see, and the sheet has header styling problems. The content underneath is detailed and correct to its own analysis, but the artifact my channel reads is not skimmable and I would reformat it before anyone looked.

5. Instruction following - 5/7

It walked the explicit constraints well. Read the whole chain across all three layers, showed layer-by-layer resolution with which layer won each header, applied the four gap types and the preflight check in both directions, used the scoring rubric, keyed the upsert on path plus method, preserved human Owner/Status, marked the vanished route Resolved, gave every Unresolved row a stated reason, held Linear to the 45 threshold with all qualifying routes linked, posted once after the writes, and showed the self-audit. It also documented the CODEOWNERS limitation honestly (Linear only exposes the signed-in user, so it left issues unassigned rather than misassigning). The prompt explicitly sanctions marking unresolvable routes rather than guessing, so the Unresolved-heavy output is within instructions even if I think it over-applied it. Docked for the header styling and the extra sheet columns beyond the seeded set.

6. Collaboration, autonomy, and verification - 5/7

Steering needed: none. It found the signed-in Linear browser session and used it without asking, which is a step up from needing to be pointed at Chrome.

Additional editing before I'd use it: moderate. I would reconcile the Unresolved-everything stance, re-examine the newsletter control and whether the Nginx-absent routes are really unresolved versus app-layer-resolved, and reformat the Teams post.

Verification is the standout. It ran a real runtime test to confirm header duplication instead of assuming precedence, pinned the commit through every check, showed a self-audit that reconciled ranks, counts, and duplicate-freedom, and specifically re-verified its one wildcard-on-authenticated claim so it would not false-positive there. What it did not do is question whether its own default-to-Unresolved posture was too conservative, which is the judgment that would have caught the over-hedge.

7. Citation quality - 6/7

Best-grounded dimension. Every route cites file and line for each contributing layer, the configured policy per layer, which layer won each header, the score breakdown, the commit hash, and the origin and request counts pulled from the log sheet. The duplicate-header claim is backed by an actual runtime observation, not an assertion. A second engineer can trace nearly every line back to a file or a log row. The one soft spot is that the runtime check ran against a synthetic install rather than the repo itself, so those specific duplicate claims rest partly on a reproduction.

8. GUI action correctness - 5/7

It drove Linear through the browser because there was no API connector, creating eight issues, updating two, and restoring one, all landing in the correct final state with no wrong-target actions. Competent, correct end state, nothing thrashed. Mid-high rather than top because the browser work, while clean, was straightforward issue CRUD rather than anything intricate.

=====================================================================================================
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
=====================================================================================================


Model - B 

Session Id : 019f9375-d1a9-7601-b1c6-0664273bb382 

1. Overall task success - 4/7

Everything landed after one steer, and it nailed the orders headline the persona explicitly flagged as the win, which is real value. But it did so while systematically under-scoring the rest of the audit. It marked policy risk "not available" on all 13 Unresolved routes, including gaps that need no edge resolution at all: PUT /api/users/:id/role is a handler that hand-writes ACAO: * on a credentialed route, a pure app-layer fact, and B scored it 37 (0 policy + 25 data + 12 reach) instead of committing the 45 wildcard risk, so it fell below the 45 threshold and got no Linear issue at all. That is one of the wildcard-on-authenticated routes the persona said they'd "get asked about first," and B left it untracked. The knock-on effect: B opened issues for 5 routes where A opened 11, purely because the policy-hedge pushed routes under the line. Add an avoidable steer, audit verdicts written into the human Status column, and a dense Teams block, and the headline win doesn't carry the whole run.

2. Task accuracy, ignoring speed - 4/7

Mechanics are solid: window filter correct (it reports 1,230 of 1,330 rows in-window and kept the 7th out-of-window origin off /api/public/config), arithmetic sums cleanly in every row, reach and data tiers mostly right, human Owner/Status preserved on the orders and products prior rows, idempotent Linear. Its orders resolution is the best of the two and its preflight reads are sharper than A's (it caught that DELETE /api/sessions/:id has OPTIONS falling through to the later global cors() advertising the wrong methods, rather than "no preflight at all"). The accuracy damage is the scoring conservatism and its ranking consequences. GET /api/users/:id has a real dead-entry (staging-old in the Express allowlist), which is fully determinable from config plus logs and needs no Nginx; B scored it 45 and marked the 15 dead-entry policy "not available (edge unresolved)," where A correctly scored it 60. Same pattern buries role, sessions, and payments. The prompt says score the components you can and don't let a missing field push a route off the list; B let "edge unresolved" suppress app-layer-certain policy on gap after gap. Smaller concrete errors: it linked the closed SAH-7 into the /api/products row even though products scored 25 and is sheet-only, and it scored products data 5 (public) where the handler serves catalog/business data. Mirror image of A on accuracy: A committed fuller app-layer scores but missed the orders headline; B nailed the headline but skewed the ranking the persona demands be reproducible.

3. Efficiency - 5/7

End-to-end time (minutes): ~13 

Wrong actions / recovery: No wrong turns, no retries. One avoidable pause.

    Fast and linear, roughly half A's wall-clock. Worth noting some of that speed comes from doing less analytical work: hedging 13 routes to "policy not available" is cheaper than resolving and defending an app-layer score for each, which is exactly what A spent its extra minutes on. The runtime Next-14 diagnostic was on-path and justified. The only drag is the confirmation stop, which is a pause rather than a wrong action, so it barely touches efficiency.

4. Writing quality - 3/7

The Teams post is a dense single-block wall of text held together by bullet separators and em dashes, which fails the prompt's explicit "keep it skimmable," and you flagged it plus a sheet styling issue. The underlying content is actually well-organized, the confirmed-versus-candidate separation in the narrative is clear and the self-audit table reads cleanly, but the artifact the channel actually sees is not skimmable. Same failure mode as A.

5. Instruction following - 4/7

Most constraints are met: all three layers read, request-order resolution with unresolvable routes marked, four gap types with preflight both directions, upsert keyed on path plus method, human Owner preserved, legacy route marked Resolved, self-audit shown, Teams posted once after the writes, stop-if-unreachable honored, and it correctly did not false-flag the login control as a wildcard. But there are concrete deviations that pull it below A. It wrote its own audit verdicts (Unresolved, Open) into the human-managed Status column on rows humans hadn't filled (cancel = Open, users/:id = Unresolved, sessions = Unresolved), a column the prompt says to leave alone; A avoided this by adding a separate audit-state column and keeping Status clean. The "policy not available" over-application runs against the explicit instruction to score what you can and not let a missing field drop a route off the list, and it did drop role off the issue list. It linked SAH-7 into a sheet-only row. And the Teams post is not skimmable. More discrete deviations, so one notch down.

6. Collaboration, autonomy, and verification - 4/7

Steering needed: It paused before the Linear writes for confirmation and, in the same message, asked how the CODEOWNERS teams map to Linear assignees, then after a one-word "yes" answered that question itself with the exact default it could have used unprompted (sole visible user plus a noted limitation). A hit the identical single-assignee wall and handled it autonomously. Part of the pause may be a genuine browser-submit confirmation requirement, which softens it, but the assignee question was self-resolvable.

Additional editing before I'd use it: Re-score the app-layer gaps to un-bury role and the dead-entries, clean the audit verdicts out of the human Status column, drop the SAH-7 link on products, and reformat the Teams post.

Verification is a real strength and the reason this isn't lower. The self-audit is honest and specific: it separates confirmed edge findings from app-layer candidates rather than overclaiming, checks duplicate keys and threshold linkage, re-fetches the posted Teams message to match counts, and openly states the Nginx topology only proves the two orders paths so it will not present an app header as the edge policy. What it did not self-catch is that its own "not available" rule was suppressing scorable app-layer risk and burying the persona's top-priority finding.

7. Citation quality - 6/7

The best-grounded dimension, on par with A. Every route carries file:line for each contributing layer, the winning-layer-per-header breakdown, and the log origins and counts, and the orders finding is backed by the Nginx headers-module inheritance rule, MDN references for multiple-ACAO and credentialed-wildcard, and a local Next-14 runtime diagnostic for the duplicate-header behavior. Fully auditable. The runtime check ran against a synthetic reproduction rather than the deployed repo, so the duplicate claims rest partly on that repro.

8. GUI action correctness - 5/7

Drove Linear through the browser correctly and with the lightest footprint of the runs: updated SAH-5 and SAH-6 and created just SAH-53 through SAH-55, right titles, no visible duplicates, correct final state, and it correctly read the assignee picker as exposing only one user. No misclicks or wrong-target actions, and the pause before submitting was an autonomy choice, not a GUI error. What keeps it at a solid mark rather than higher: with only three creates there was little on-screen navigation to grade, and it signed off on the set from a single all-issues list scan rather than re-opening any issue to check its body, labels, or PR link.



=====================================================================================================
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
=====================================================================================================


Model - C 

Session Id : 019f9390-30b4-77c1-bf63-d5923f33922c


1. Overall task success - 4/7

It resolved the effective policy for 14 of 15 routes and marked only GET /api/status Unresolved, which is exactly the single route the setup designed to be unresolvable. A and B each punted 13 routes to Unresolved on "Nginx front door unprovable"; C committed to the reading that a route with no Nginx location is served with the app layer as its effective policy, and resolved them. For a persona whose entire ask is "the effective policy after all three layers fight it out," that is the most valuable outcome of the three. It also committed real app-layer policy scores (so 11 routes cleared the threshold and got issues, like A, not B's 5), kept the human Status column clean (unlike B), and correctly left SAH-7 off the products row (unlike B). What holds it at a 4: it badly under-handled the orders headline (below), it stopped at preflight and declared Linear "unreachable" without checking the signed-in Chrome session that both A and B found on their own, and the back half took 21 minutes.

2. Task accuracy, ignoring speed - 5/7

It resolved 14/15 routes and committed the correct app-layer policy scores across the board: dead-entry 15 on GET /api/users/:id (score 60, where B wrongly marked it "not available" and got 45), split-override 25 on /api/profile (62), wildcard-logged 35 on /api/public/config (60), wildcard-authenticated 45 on the one genuine case PUT /api/users/:id/role (82, rank 1). Tie-breaks are applied correctly (the three 55-point rows order orders, checkout, cancel by descending traffic), the window filter is right (public/config shows 6 origins and 5,000 requests, the 7th out-of-window origin correctly excluded), and the arithmetic sums cleanly. It got the two preflight controls exactly right: R02 payments as a POST-advertised/DELETE-implemented mismatch, and R11 sessions as genuinely preflight-missing (Express returns Allow: DELETE with no Access-Control headers), which is the precise reading. The concrete accuracy miss is the headline: on POST /api/orders it described the mechanic correctly (location defines add_header, drops server inheritance, adds ACAO *) but then tagged the gap only as "silently blocked + preflight mismatch" and scored policy 10, missing both the split-config override and the wildcard label the seed's R01 is built around, so it ranked the persona's marquee route 5th at 55. Same under-tagging on checkout. 

3. Efficiency - 4/7

End-to-end time (minutes): ~23 

Wrong actions / recovery: It declared Linear "unreachable" at preflight and asked you to connect it and rerun, when the Chrome session was already logged in.

After the unblock it ran clean and steady with no dead ends, but the 21-minute back half is long and the premature stop cost a round-trip that better preflight would have avoided. Slower-feeling than B by a wide margin and it needed a steer A didn't, so below both on this dimension.

4. Writing quality - 3/7

The Teams post is a dense single-block wall of text with em-dash separators, which fails the prompt's "keep it skimmable," and the sheet header styling plus the sparse look. The per-row CSV reasoning and the self-audit are clear enough, but the delivered channel artifact is not.

5. Instruction following - 4/7

It followed the central instruction : state the effective policy that reaches the browser, resolving the resolvable routes instead of hiding behind Unresolved, and it kept the human Owner/Status columns clean rather than overwriting them. Threshold and linkage are right (11 routes at 45+, each with one open issue), the legacy route is marked Resolved, the self-audit is shown, and it verified the wildcard-authenticated finding against headers and credentialed logs without false-flagging login. The deductions are concrete: it left the orders and checkout gap-types missing the split-config override (a classification miss on the two most important multi-layer routes), it under-scored orders at policy 10, and its stop-if-unreachable call misfired by declaring Linear unreachable before checking the browser fallback.

6. Collaboration, autonomy, and verification - 4/7

Steering needed: It declared Linear unreachable without checking the signed-in Chrome tab, so it needed to point out the session before it could proceed.

Additional editing before I'd use it: Re-tag orders and checkout as split-config/wildcard, reformat the Teams post, and decide whether the Owner column should carry the CODEOWNERS team.

After the unblock it ran the whole back half unattended, and its verification is genuinely strong: it validated the two framework traps directly (the Next.js overlapping-header override and the Nginx inheritance drop) rather than asserting them, verified the wildcard finding against both emitted headers and credentialed log rows, and ran a self-audit covering coverage, ranking, uniqueness, linkage, Teams-count match, and human-cell preservation. What it did not self-catch is that it described the orders mechanic correctly but then scored and tagged it wrong.

7. Citation quality - 5/7

Grounded and auditable: file:line for every layer, origins and counts from the log sheet, the winning-layer-per-header breakdown, and direct framework validation behind the duplicate-header and override claims. It doesn't bring in the external authoritative references and the winning-layer detail is terser on several rows. Solid grounding, just not the best-documented of the set.

8. GUI action correctness - 5/7

Drove Linear through Chrome correctly across the heaviest issue count so far: created SAH-56 through SAH-64 and updated SAH-5 and SAH-6, all with the right titles, no visible duplicates, correct final state, no misclicks or wrong-target actions. What keeps it at a solid mark rather than higher: the nine creates were the same form filled in nine times with no tricky navigation, and once they were in it confirmed the batch by scanning the all-issues list, never re-opening a single created issue to eyeball its body, labels, or PR link on screen. Correct and clean, but repetitive and list-verified.

=====================================================================================================
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
=====================================================================================================


Model - D 

Session Id : 019f93b3-8179-7622-a69e-24edb3115ad0

1. Overall task success - 5/7

It maps directly onto the persona's stated priorities. It's the only run that identified all four wildcard-on-authenticated routes the seed built (orders 90, checkout 90, role 82, admin/export 75). It got the orders headline right and tagged it most completely (wildcard-auth + split-config + duplicate-ACAO + preflight-mismatch). It's the only model that kept GET /api/admin/export above threshold at 75 by scoring the handler's credentialed wildcard as policy 45, exactly the "must not be dropped even with no traffic" case the seed designed. Its silent-block count is the most accurate of the four (2 genuine origin-mismatch routes, cancel and products, versus A's 6 and C's 7 inflated by the duplicate reading). Thirteen issues, correct tie-breaks, the most rigorous citations. What holds it at 5 rather than higher: it needed a confirmation-gate steer before the writes, a round-trip a fully autonomous run avoids; it marked 13 of 15 routes Unresolved on the front door even though it found no catch-all proxy to justify that much hedging; it wrote its own Unresolved verdicts into the human-managed Status column it was told to leave alone; and its 14-Critical bucket is heavy even with a stated cutoff. Those four are the gap between a strong run and a clean one.

2. Task accuracy, ignoring speed - 6/7

The most accurate run, and the gap over the others is concrete. All four wildcard-auth routes correctly classified and individually verified against emitted headers and credentialed log counts (orders 4,200 credentialed, checkout 2,600, role 260, admin/export flagged traffic-unknown rather than faked). It committed every app-layer policy score the seed intended: dead-entry 15 on users/:id (60), split 25 on profile (62), wildcard-logged 35 on public/config (60), and the four wildcards at 45. It read products as business data (15) rather than public (5), which matches the seed, and its silent-block taxonomy is the cleanest, reserving the label for genuine origin mismatches instead of tagging every duplicate-header route as blocking everyone. Window filter correct (1,230 in-window rows, the 7th out-of-window origin excluded), arithmetic clean, tie-breaks all correct, human Owner/Status preserved. Residual dings keep it off 7: the 13/15 Unresolved is more hedged than the seed's single intended unresolved route, the Status-column pollution, products nudged to exactly 45 via a preflight-mismatch tag that's borderline, and the shared newsletter-control false flag. But on the analytical core the persona cares most about, this is the run that actually landed it.

3. Efficiency - 5/7

End-to-end time (minutes): ~13.5 

Wrong actions / recovery: No wrong turns, no retries. One avoidable pause for write-confirmation.

Fast and dense, roughly half of A's and C's wall-clock while producing more complete analysis than either. The local Next runtime check was on-path and, unusually, caveated. The only ding is the confirmation stop before writes and it's a pause rather than wasted work.

4. Writing quality - 3/7

The Teams post is a dense single block with em-dash separators, which fails "keep it skimmable," and you flagged it. Worth noting the underlying narration and CSV are the best-written of the four, the preliminary analysis is well-structured and sourced and the self-audit is clear, but the artifact the channel actually reads is the same wall of text.

5. Instruction following - 5/7

It followed the hardest instruction, the scoring rubric, most faithfully: it surfaced all four wildcards, kept admin/export at 45+ exactly as the seed's scoring section demands, and applied the tie-break order correctly through all fifteen rows. It checked the browser fallback at preflight rather than prematurely declaring Linear unreachable, read all three layers with the fullest layer-by-layer resolution, held Linear to the threshold with all thirteen qualifying routes linked, preserved the two human-filled cells, marked the vanished route Resolved, posted once after the writes, and showed a thorough reconciled self-audit. Deductions: it wrote its own Unresolved verdicts into the human-managed Status column, added a rank column beyond the seeded seventeen, and the Teams post is not skimmable. On the analytical instructions it's the strongest of the four; the peripheral deviations are what hold it at 5.

6. Collaboration, autonomy, and verification - 5/7

Steering needed: once. It checked the fallback and confirmed all four systems itself, then paused before the writes for confirmation plus the assignee mapping, while offering the leave-unassigned default it ultimately used. A did the same writes without stopping, so the pause was avoidable, but it was a clean "here is my complete analysis, approve the writes" checkpoint, not a half-done stop.

Additional editing before I'd use it: Clean the Unresolved verdicts out of the human Status column and reformat the Teams post.

Verification is the best of the four and the reason this scores where it does. It verified each of the four wildcard-auth findings individually against headers and credentialed request counts, cited authoritative sources for every mechanic (Nginx headers module, MDN multiple-ACAO and credentialed-wildcard, Next.js header-override and execution order, and the expressjs/cors source for why an earlier global OPTIONS terminates a later route-level one), and ran a self-audit reconciling coverage, terminal states, threshold linkage, duplicate keys, ranking tie-breaks, and Teams counts. What it did not self-catch is the Status-column pollution or whether its own no-catch-all finding should have let it resolve more rows.

7. Citation quality - 6/7

The best-grounded of the four. Every route carries file:line, origins, counts, and a winning-layer breakdown, and the mechanics are each tied to a named authoritative source, including the expressjs/cors implementation detail the others didn't cite. The four wildcard verifications each cite their credentialed request count, and the runtime check is explicitly caveated for the missing lockfile, which is the most careful handling of reproducibility in the set. Not a 7 only because the duplicate-header claims still rest partly on that synthetic runtime observation, even well-flagged.

8. GUI action correctness - 5/7

Drove Linear through Chrome correctly, and this run had the one bit of on-screen judgment in the set: alongside updating SAH-5 and SAH-6 and creating SAH-65 through SAH-75, it opened a fresh products issue next to the older Done SAH-7 without duplicating it, all with right titles and correct final state, no misclicks or wrong-target actions. What keeps it at a solid mark rather than higher: even that products-versus-SAH-7 call was handled as routine CRUD, and it confirmed all eleven creates from the all-issues list rather than re-opening any of them to verify body, labels, or PR link on screen. Correct and clean, but list-verified.

=====================================================================================================
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
=====================================================================================================
=====================================================================================================
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
=====================================================================================================


Ranking: D > A > C > B

Which model is best overall: D

D is the only run that delivered the audit the setup was designed to extract. It surfaced all four wildcard-on-authenticated routes, the exact finding the persona said they'd be asked about first, where every other model found one and buried or dropped the rest. It got the orders headline at 90 with the full tag set, it was the only model to keep admin/export above threshold per the seed's explicit "must not be dropped" instruction, its silent-block taxonomy is the most accurate, its ranking is the most seed-aligned with correct tie-breaks throughout, and its citations are the most rigorous, down to caveating the runtime check for a missing lockfile. It cost one avoidable confirmation stop and it polluted the Status column, but the analytical completeness is in a different tier.

A is second. It's the cleanest process of the remaining three: fully autonomous with zero steers, it kept its Unresolved verdicts in a separate audit-state column instead of touching the human Status column, it committed the app-layer scores that produced eleven tracked issues, and it's the best-documented after D. Its analytical ceiling is lower, it read the orders headline as split-config at 70 rather than the intended wildcard and found only the one role wildcard, but it ranked orders second and tagged it, so its audit still surfaces the priority routes in a usable, reproducible form.

C is third, and it was close with A. Its real strength is that it resolved the effective policy for fourteen of fifteen routes and marked only status Unresolved, which is the single thing the persona said they value most, where A, B, and D all hedged thirteen rows to Unresolved. It also committed the app-layer scores (eleven issues) and kept the human columns cleanest. But it fumbled the named headline worst of the four, tagging orders only silently-blocked plus preflight-mismatch, missing both the wildcard and the split-config, and ranking it fifth. It also stopped at preflight and declared Linear unreachable without checking the Chrome session that A, B, and D all found on their own, and it was the slowest run. The resolution breadth nearly pulls it level with A; the premature stop and the headline miss tip it just behind.

B is last, despite getting the orders headline right at 90, because getting one finding right did not save an audit that under-delivered everywhere else. Its blanket "policy not available" on the Unresolved routes buried PUT /api/users/:id/role, a clear credentialed wildcard and a top-priority finding, at 37 with no issue at all, and cut the tracked-gap set to five issues where the others opened eleven to thirteen. It also polluted the Status column, linked the closed SAH-7 into a sheet-only products row, and needed a steer. The persona asked for a reproducible ranking and the wildcard routes surfaced first; B's ranking dropped one of them off the issue list entirely, and that outweighs the single headline it landed.

One pattern across all four to send back: every model produced a dense, em-dash-heavy Teams post that failed the explicit "keep it skimmable" instruction, so writing quality was a flat 3 across the board and did nothing to separate them. If the next cycle wants signal there, the Teams formatting is a reliable place all four currently fall down. The other shared artifact is the newsletter control: all four flagged it via the duplicate-header reading, so if it was meant as a clean don't-cry-wolf route, none of them held the line on it.