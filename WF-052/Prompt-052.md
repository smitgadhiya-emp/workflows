Dig into the API surface of the backend in GitHub repo `keyurempiricinfotech-art/test-repo` under `apps/backend` (branch `main`). I want to know which endpoints have basically become dead weight without anyone noticing. The point is to pay down some tech debt. But I do not want us breaking prod to do it, so err on the cautious side the whole way through. Use the connected plugins (GitHub, Google Sheets/Drive, Jira, Microsoft Teams) and the existing Chrome session if it helps, or open a fresh one. Every date I mention is Asia/Kolkata.

Treat this the way a backend engineer with a few scars would. An endpoint being quiet is not a reason to kill it. Some of our most important routes barely get called. So before you flag anything for retirement, go look at how it's actually used, what leans on it, and what falls over if it's gone. Take your time per endpoint. No shortcuts. And I mean every endpoint. If you sample, summarize, or stop early because the pattern "looks clear," that's a fail. Coverage has to be total or the whole thing is worthless.

Here's what I actually want out of this:

A running inventory of every API we expose. A real sense of how each one gets used in prod, not how we assume it does. The ones that stopped pulling their weight. And a few safe, boring, low-drama removals that won't blow up somebody's integration.

Nothing becomes a removal candidate off a single signal. I want the traffic, the code deps, the docs, and the client usage all four checked, and I want to see all four in the record before that call gets made. Three out of four is not enough. If any one of the four is missing or unclear, it cannot be a removal candidate, it goes to review instead.

Mapping the surface : Start from the latest commit on the default branch (`main`) of `keyurempiricinfotech-art/test-repo`. Confirm which branch is default and note the commit you read from, so this can be rerun against the same baseline. Then go find everything we expose under `apps/backend`. REST routes, GraphQL queries and mutations, webhooks, the public stuff, the internal stuff, and anything that's been versioned. For each one, grab the route and method, the controller, service, module, and repo path, whatever auth it needs, its version, and whether it's public or internal. If one logical endpoint has two or three versions live, don't lump them. Each version is its own record. Treat the composite of route plus method plus version as the identity of a record. Two rows are the same endpoint only if all three match. Same path on GET and POST is two records. v1 and v2 of the same path is two records.

Production usage : The monitoring data sits in the `mock api data` Google Sheet (in the `Engineering / API Lifecycle` Drive folder), covering May 1 through June 29, 2026 (Asia/Kolkata), which is the full 60-day window I keep referring to. Pull total requests, unique clients, error rate, average latency, peak traffic, the last time it was hit, how requests were spread out, and the auth method. Throw out the fake traffic first, before you count anything. Fake traffic means health checks, monitoring probes, synthetic checks, load tests, and uptime pings. In practice that's any caller whose client or user agent reads as a health, probe, monitor, synthetic, loadtest, k6, uptime, pingdom, or status checker, plus any hits to health, healthz, ping, status, readiness, liveness, or metrics style paths that carry no real auth. Strip those rows, then every number you report (total requests, unique clients, last hit, peak, spread) is the real-caller number only, computed after the strip. Say how many rows you stripped per endpoint. And here's the catch: if you cannot confidently tell whether a source is synthetic or a real caller, do not drop it and do not silently keep it either. Keep the request in the count and mark that endpoint Needs Review for dirty traffic. Guessing a source is fake so an endpoint looks dead is exactly the mistake I'm trying to prevent.

Lining code up against traffic : Now cross them. Sort each endpoint into exactly one usage bucket off the real-caller counts, using these lines so two people scoring this land in the same place: never called is 0 real requests in the window, near zero is 1 to 9, low usage is 10 to 24, deprecated but live is anything flagged deprecated in code that still shows 1 or more real requests, internal only is not public and only called by internal clients, legacy version is a superseded version that still has a newer version live. If an endpoint could fit two buckets, list every bucket it qualifies for and pick the one that carries the most caution (deprecated-but-live and legacy-version outrank the low-count buckets, because a thing that still gets hit is more dangerous to touch than a thing that's simply quiet). Whatever bucket something lands in, tell me why, in one line, tied to the actual number. And I'll say it again because it matters: volume alone doesn't decide anything past the bucket label. Dependencies get checked before you breathe a word about deleting anything.

Grep the whole `test-repo` monorepo for references first not just `apps/backend`. Front-end (`apps/web`), mobile (`apps/mobile`), internal services (`services/*`), background workers and cron jobs (`services/notification-worker` and friends), event consumers, webhooks, SDKs (`packages/sdk`), shared libs (`packages/shared`), public docs, and the OpenAPI/Swagger spec (`apps/backend/docs/openapi.yaml`). All of it. For each endpoint, log where you looked and what you found, including the surfaces that came back empty, so a clean bill of health is something you can point at, not something you assumed.

If something's actively using it, it stays. Full stop. And if you honestly can't tell whether a dependency exists, for example a dynamically built route string, a reference behind a config value, or a call you can't fully resolve, don't fill the gap with a guess. Kick it to manual review and write down exactly what you couldn't resolve.

Who's still calling : Figure out the callers per endpoint. Web app, mobile, an internal service, a third party, a partner, or just unknown. Note how many clients, the last one you saw, the last request time, and their version if that's available. When traffic could belong to more than one live version and the data doesn't clearly tie a hit to a specific version, do not split it by feel. Attribute only what the data actually pins to a version, and mark the rest unattributed and Needs Review. Can't ID all the callers? Say so, plainly, and say how many are unidentified. I would rather have an honest gap than a confident fiction.

Removal risk : Three buckets, and only three.
Safe to Remove is a high bar. Every one of these has to be true at once: zero real prod traffic in the window, zero code deps found across every surface you grepped, no documented integration in any spec or doc, and no known client. Miss any single one, or have an unresolved gap in any single one, and it does not qualify, no exceptions. A deprecated endpoint that still takes real traffic can never be Safe to Remove. An endpoint with zero traffic but a live code dependency can never be Safe to Remove, it's Keep. Needs Review is where most of the fuzzy stuff goes: traffic under 25 real hits with any hole in the dep, doc, or client picture, a legacy integration that might still be lurking, dirty traffic you couldn't clean, unattributed version traffic, or nobody's sure who owns it. Keep is the easy one: active real traffic, an internal system depends on it, public clients rely on it, or it's part of a version we still support. Whichever bucket, show the four-signal reasoning (traffic, deps, docs, clients) that got you there. I want to follow the logic, not just see the verdict.

Tech debt score: Score the debt objectively so it's repeatable, don't eyeball it. Rate each of these seven factors from 0 (none) to 3 (severe): age or legacy status, how little real usage it gets, code gnarliness, maintenance cost, security exposure (public and weak or no auth pushes this up), documentation overhead, and how many things depend on it. Add them for a total out of 21. Map it: 0 to 5 is Low, 6 to 11 is Medium, 12 to 16 is High, 17 to 21 is Critical. Show the per-factor numbers and the total next to the band, so I can see exactly how you got there.

Keeping the records clean : Update the `api workflow test` Google Sheet (same Drive folder). Match existing rows on the route plus method plus version key and edit those in place. Do not spawn duplicates, and never create a second row for a key that's already there. Only add a new row when no existing row matches that key. Each row needs the endpoint, method, module, version, last prod call, total requests over the 60-day window, unique clients, removal risk, debt score, recommended action, owner, and status. Status has to be exactly one of: Active, Candidate for Deprecation, Pending Review, Approved for Removal, Removed. And leave the ownership column alone unless I tell you otherwise, even if it's blank.

For Jira, anything you mark Candidate for Deprecation gets an issue in the `api-reporting` project. One issue per endpoint, and check the project first so you don't reopen a duplicate for an endpoint that already has one. Put the endpoint details in there, where it lives in the repo, the usage summary, the last prod request, the client and dependency analysis, the risk assessment, a suggested way to deprecate it, a validation checklist, and acceptance criteria. Tag them technical-debt, api-cleanup, backend, architecture, deprecation. Assignee stays empty unless I say so.

Then the report. Create a new Google Sheet titled `API Technical Debt Report Months-Year` where Months-Year is derived from the analysis window itself, not hardcoded (for this window that resolves to `API Technical Debt Report 05-06/2026`). Put it in the `Engineering / API Lifecycle` Drive folder. I want an exec summary up top, then the counts: total reviewed, active, unused, low-usage, and the deprecated ones still somehow taking traffic. After that, the removal candidate list, the high-risk dependencies, a rough estimate of maintenance we'd save, security wins, and a cleanup roadmap. Every number in the report has to reconcile with the `api workflow test` tracker rows, if a count in the report doesn't match the rows, fix it before you post. Lean on the numbers you found, not vibes.

Last piece is the Teams post. Summary goes to the `Workflow test` channel in the `Workflow test` team, with a link to the report. Include total reviewed, safe removal candidates, the ones needing manual review, deprecated endpoints still getting hit, the oldest unused one, the estimated debt reduction, how many Jira issues you opened, and the report link. One more thing on this. If some old or deprecated endpoint is still being used by actual people, pull it out into an "Immediate Engineering Review" section and make it loud, up top. The team should catch an at-risk live endpoint in one glance, not by scrolling. If there are none, say that plainly too so nobody wonders whether you checked.

Ground rules, in case any of the above gets ambiguous

Cleanup never wins over safety. Thin evidence means no delete recommendation, period. Keep what you verified separate from what you're guessing, and label which is which in the record. When you're missing something, name exactly what's missing and mark it Needs Review. Don't invent anything, not how a thing works, not what it depends on, not who owns it, not where data lives, not when something happened. And any time your confidence is low, tell me why it's low in that endpoint's row or note.

What finished looks like:
At the end I want the full inventory, the usage analysis, the dependency analysis, the client analysis, the debt assessment, the removal recommendations, an updated Lifecycle Tracker (`api workflow test`), the Jira issues for deprecation candidates, the report, and the Teams summary.

It's not done until every endpoint you found has been looked at, matched against usage, checked for dependencies across every surface, the uncertain ones parked for review with the reason named, and every report and ticket is actually in place and reconciles with the tracker.

If GitHub, the Google Sheets/Drive, Jira, or Teams can't be reached, or the `mock api data` sheet is missing/empty for the window, stop and tell me exactly what's unreachable rather than guessing or half-finishing don't post a partial report or an incomplete Teams summary.


Metadata:

1. Occupation / career (dropdown choice):
-> Software Developer
2. Occupation + workplace (one line, this is the persona voice):
-> Backend/platform engineer at a software company that runs a monorepo-based product (backend services plus web and mobile apps), responsible for API maintenance and technical-debt management.
3. Time to complete this workflow WITHOUT a model (minutes):
-> 850 minutes  
4. Times PER MONTH I run this workflow (decimal ok, 0.5 = every 2 months):
-> 0.5
5. Workflow difficulty 1-7 (1 easy, 7 hard):
-> 6