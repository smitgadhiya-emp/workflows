The Postgres database keeps throwing slow queries and every time it happens someone burns half a day figuring out which service actually caused it. We've got a bunch of things hitting the same database the Node backend, some Python workers, Supabase edge functions, cron jobs, a GraphQL server so when the DB says a query took 6 seconds, nobody knows where it came from. Trace the slow queries automatically and give the engineering team a report that already has the answers. I want the hard version of this, not a surface pass. If two people ran this on the same data they should land on the same 20 groups, the same source files, the same owners, and the same priorities. If your output isn't reproducible, it's wrong.

The run must cover the slowest Postgres query groups and produce query details, source tracing, root causes, business impact, priorities, fixes, Jira tickets, tracker updates, and a Teams summary. Pull from pg_stat_statements and the slow query log and take the top 20 groups, but "top 20" is by total database time (mean_exec_time × calls), not by single slowest execution, because one 8 second query that ran twice matters less than a 400ms query that ran 90,000 times. Tie-break by users impacted, then by max duration, then by query hash so the ordering is deterministic. For each group include the query text, execution count, average, max and min duration, last execution time, database, user, and query hash, and where the data has it, rows returned vs rows scanned, planning time, buffers, and lock/wait info. If a field is missing in the source, say "not available" rather than guessing a number, and don't let a missing field silently drop a query out of the top 20.

Normalize the SQL before anything else, and this is where most tools get lazy so don't. Strip literals, dynamic IDs, timestamps and UUIDs, collapse whitespace and casing, drop SQL comments, and fold the things that look different but aren't: an IN list with 3 values and the same IN list with 40 values are one pattern, `= ANY($1)` and `IN (?, ?, ?)` are the same shape, a multi-row VALUES insert of any length is one pattern, and already-parameterized statements ($1, $2 or ?) fingerprint the same as their literal-filled twins. `WHERE user_id=9812` and `WHERE user_id = $1` and `where USER_ID = 4471` all have to land on the same fingerprint. Then group by that fingerprint, not by raw text, and give each group a plain-English name with its numbers, like "User Lookup Query ran 18,400 times, averaging 2.4 sec." Assign each group a stable Query ID derived from the fingerprint hash so the same query gets the same ID on every run and the tracker can upsert instead of duplicating.

Now the part that actually matters, tracing each group back to where it lives in code. Search the monorepo keyurempiricinfotech-art/db-performance on branch main backend services under api-node/, workers-python/, supabase-functions/ and the frontend under web-storefront/, admin-panel/, Supabase project acme-storefront (ref acmestorefront), Postgres acme-prod-pg database acme_production in production, Jira project DBP, monitoring in the "Monitoring & Traces Data" Google Sheet (stands in for Datadog, New Relic and Grafana), the tracker Google Sheet "Database Query Performance Tracker" (Drive folder Engineering / DB Performance), and Teams channel Workflow test > cross check query origin. Here's the hard part: the SQL in pg_stat_statements is what Postgres received, but the code almost never contains that string literally, because TypeORM, Prisma, Sequelize, Knex, SQLAlchemy and the Django ORM all build SQL at runtime. So you can't grep for the query text. You have to reason from the shape which tables and columns it touches, which joins, which filters and infer the ORM call that would generate it, then confirm by reading the surrounding code. In Node that's a repository method like UserRepository.findUserByEmail(), a TypeORM/Prisma/Sequelize/Knex builder chain, or a raw db.query(...). In Python it's SQLAlchemy, Django ORM, psycopg or asyncpg. In Supabase it's an edge function, an RPC, or a SQL procedure. And be honest that one normalized query group can have more than one origin the same `SELECT * FROM users WHERE email = ?` might be generated from four different call sites across three services, so map one-to-many when that's the truth, don't force a single answer. For every origin give the exact file and line and a confidence level (high / medium / low) with the evidence you used. If confidence is low, treat it as not fully traced, not as done.

Once you've found an origin, trace upward through the call graph. Repository to service to controller to the actual API route, so the chain reads POST /orders/create goes into OrderService, which hits InventoryRepository, which fires the slow query. Then one more hop into the frontend to find which screen calls that endpoint, so if it's the checkout page I know real users feel this on checkout. Where the call graph forks (a shared repo method called by several services) trace every branch, not just the first one you hit.

For each group work out why it's actually slow, and back it with evidence from the plan or the stats, not a vibe. Missing index and a sequential scan over millions of rows, an N+1 where one parent query fans out into thousands of child queries, a full table scan, an expensive nested join, an ORM pulling whole objects it doesn't need, missing pagination returning 150k rows, the same query fired hundreds of times inside one request, lock waits, deadlock risk, connection pool saturation. N+1 in particular you can't guess it, you confirm it by correlating the child query's call count against the parent request count in the trace data, so show that correlation. A query can have more than one root cause, so list all that apply and mark which one dominates.

Then estimate business impact by joining the code trace to the monitoring data. Use the "Monitoring & Traces Data" Google Sheet for endpoint latency, frequency, error rate and peak hours, and the traces tab of that same sheet for spans, over the window 2026-06-06 through 2026-07-05 inclusive. Join on API route (fall back to service name when the route is missing), and when a query maps to several endpoints, attribute its impact across all of them rather than picking one. Report which API, requests per hour, users hit, whether it touches checkout or payments (that's revenue), the latency it's adding, mobile impact, and whether it gets worse at peak. If a query group has no matching monitoring row, say so plainly it's an internal or background query with no user-facing trace, and it still gets ranked, just flagged as impact-unknown.

Prioritize from there and apply the rules consistently. Checkout, payments, auth, or a high-frequency API running 5+ seconds is critical. Customer-facing and frequently-run queries are high. Internal dashboards and background workers are medium. Admin and rare maintenance stuff is low. When a query hits both a critical and a low path, it takes the higher one. Write the one-line reason each priority was assigned so the call is auditable.

For each issue give real fixes matched to the actual cause, not a generic checklist. Database side that's adding a specific index (name the columns and the order), rewriting a join, partitioning or archiving, ANALYZE/VACUUM, or fixing stale planner stats. App side it's pagination, selecting fewer columns, caching, batching, killing the N+1, or fixing eager/lazy loading. Infra side it's the connection pool, Postgres tuning, a read replica, moving analytics off to it, or Redis in front. If you recommend an index, say roughly what it costs (write amplification, disk) so it's not a free lunch on paper.

Then handle Jira idempotently, because this runs repeatedly and I don't want duplicate tickets. Before creating anything, search project DBP for an existing open ticket keyed to that Query ID or query hash. If one exists, update it with the latest numbers instead of opening a second. If not, create one titled like "Optimize slow query in OrderRepository.createOrder()" with the query hash, a short SQL summary, the API route, the repo, the file path and line number, the root cause, the performance numbers, the business impact, the suggested fix, the priority, and the monitoring links. Assign it by reading the CODEOWNERS files in the repos and matching the source file path, and honor CODEOWNERS precedence (last matching pattern wins). If a path has no owner, assign to the default backend team and note that it fell through.

Keep the "Database Query Performance Tracker" sheet as an upsert, not an append. Match rows by Query ID: update the metrics, root cause, fix, priority, Jira link and Last Reviewed on existing rows, add new rows for new query IDs, and do not overwrite the human-managed columns (Owner, Status) if someone has already set them. Columns are query ID, hash, SQL summary, repository, service, API endpoint, frontend screen, source file, line number, average and max execution time, calls per day, users impacted, root cause, recommended fix, priority, Jira ticket, status, owner, last reviewed. A query you couldn't trace still gets a row, marked Not Traceable, with your best candidate files and their confidence, and which tool or data was missing.

Post the Teams summary exactly once to Workflow test > cross check query origin, and only after Jira and the tracker are done, so a half-finished run doesn't spam the channel. Group it total slow queries and the count at each priority, top impacted APIs, top root causes with counts, and what you did (tickets created vs updated, tracker rows upserted, owners notified), plus the tracker link. Keep it skimmable.

Before you call it finished, do a self-audit pass and show it. Confirm every one of the top 20 normalized groups ends in one of two states: a linked Jira ticket, or a Not Traceable tracker row with best candidates and missing tools listed nothing half-done, nothing silently dropped. Confirm no duplicate Jira tickets were created, no duplicate tracker rows exist, and the counts in the Teams message actually match the tracker. If any of those checks fail, fix it before posting, or if you can't, say exactly what's off.

One thing I'll stress the whole value is the cross-stack tracing, SQL to ORM to repository to service to controller to API to frontend screen across Node, Python and Supabase. If you can pin a query to an exact file and line and the team that owns it, that's the win. If you genuinely can't trace one, say so with your best candidates and your confidence, don't invent a location to look complete. And if any tool or data source isn't reachable Postgres, GitHub, Jira, the monitoring tell me what's missing rather than skipping it quietly.

Note: the Postgres pg_stat_statements / slow-query data is in the "Postgres Slow Queries Data" Google Sheet, and the Datadog / New Relic / Grafana / trace data is in the "Monitoring & Traces Data" Google Sheet, both in the Drive folder Engineering / DB Performance. Read those instead of the live products.


Metadata:

1. Occupation / career (dropdown choice):
-> Software Developer 
2. Occupation + workplace (one line, this is the persona voice):
-> Backend/platform engineer responsible for database performance and reliability across a multi-service Postgres-backed platform at a software company.
3. Time to complete this workflow WITHOUT a model (minutes):
-> 300 minutes
4. Times PER MONTH I run this workflow (decimal ok, 0.5 = every 2 months):
-> 2
5. Workflow difficulty 1-7 (1 easy, 7 hard):
-> 7